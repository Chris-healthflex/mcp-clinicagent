# MCP Medical API Server Requirements
# Compatible with Python 3.14+ (tested with Python 3.12-3.14)

# Core Flask
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# OpenAI
openai>=1.30.0

# HTTP Requests
requests>=2.31.0

# LangChain - Use specific compatible versions
langchain-classic>=0.3.0
langchain-openai>=0.1.25
langchain-core>=0.2.38

# LangGraph - State-based workflow
langgraph>=0.2.0

# Token counting
tiktoken>=0.5.0

# Pydantic (compatible version)
pydantic==2.9.2

# MongoDB (for BSON ObjectId in combined_processing.py)
pymongo>=4.0.0

# Note: ChromaDB is accessed via the MCP server (mcp_medical_server.py)
# so it doesn't need to be installed in this client environment